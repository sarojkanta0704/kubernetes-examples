apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: blog-ing
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: blog.amoylabs.com
    http:
      paths:
      - path: /
        backend:
          serviceName: wordpress
          servicePort: 80
