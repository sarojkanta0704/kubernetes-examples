apiVersion: v1
kind: Service
metadata:
  name: drools-workbench-svc
  namespace: drools-workbench
spec:
  ports:
    - port: 8080
      protocol: TCP
      name: http
      targetPort: 8080
  selector:
    app: drools-workbench
